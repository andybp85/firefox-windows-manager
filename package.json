{
    "description": "Toggleable full-page overview to manage Firefox windows, tab groups, and tabs.",
    "name": "firefox-windows-manager",
    "private": true,
    "scripts": {
        "package": "rm -f tab-window-manager.xpi && zip -r -X tab-window-manager.xpi manifest.json background.js dashboard.html dashboard.css dashboard-bg.svg icons src -x '.*' '*/.*'",
        "test": "node --test"
    },
    "type": "module",
    "version": "1.2.0"
}
